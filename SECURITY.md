# 安全模型

AI DB Query Gateway 是本机查询安全边界，不是数据库权限系统的替代品。建议先使用
数据库端 `SELECT`-only 账号、限制来源主机并开启 TLS，再把 AST、只读事务、rollback、
查询预算和审批视为纵深防御。

## 能保证什么

- AI Token 只能调用八个查询/元数据工具，不能调用数据源、凭据、Token 或审计管理接口。
- 所有非查询 SQL 在创建业务库 JDBC 连接前拒绝；未知语法和未知函数 fail-closed。
- 数据源不读取数据库账号授权；连接成功后统一进入 `COMPATIBILITY`，每次查询审计
  都明确标记为应用层只读。
- 连接配置和审计密钥进入 macOS Keychain；SQLite 不保存地址、用户名或密码。
- API Token 为 256 位随机值，只显示一次，SQLite 只保存 SHA-256 摘要和数据源范围。
- 完整 SQL 与参数仅保存 AES-256-GCM 密文；审计链校验失败后业务操作中止。
- 查询结果不持久化，不导出，网页按纯文本渲染。为便于管理员在审计轨迹中复核刚执行的查询，
  服务最多在进程内缓存 20 条结果 15 分钟；缓存不进入 SQLite、审计或日志，重启和到期后清除。
- 普通日志不记录 SQL、参数、数据库地址、用户名、密码、API Token 或初始化 Token。

## 明确不能保证什么

### 同一 macOS 用户的 Shell 权限

当前 Swift helper 使用当前登录用户的 Keychain。若 AI 或其它进程拥有与网关相同
macOS 用户的任意 Shell/文件执行权限，它可以直接运行 helper、读取该用户可访问的
Keychain 条目，或读取 MCP Token 的客户端私有配置。helper 的所有者、权限、符号链接
和启动后 SHA-256 校验只能防止误配置与运行期替换，不能在同一 UID 内建立真正隔离。

因此安全部署分两种：

- AI 只有作用域 MCP 工具、没有任意 Shell：当前本机架构能隔离数据库凭据。
- AI 同时拥有任意 Shell：应把网关运行在独立 macOS 用户下，不授予 AI 该用户的
  文件、进程和 Keychain 访问；更完整的后续方案是签名的 Hardened Runtime App +
  XPC helper，并通过 audit token 校验调用方。

不要向拥有同一用户 Shell 权限的 AI 宣称“它绝不可能读取凭据”。

### 兼容模式

数据库账号自身带写权限时，网关只能提供强应用层只读。驱动漏洞、数据库解析差异或
宿主机被攻陷都可能越过应用边界。当前版本不会查询 `SHOW GRANTS`、`SESSION_PRIVS`
或角色/对象授权，也不会识别 root、SYS 或管理员账号；连接失败时才会隔离数据源。

### 本地审计

链式 HMAC 能检测 SQLite 审计篡改，但不等同独立 WORM。能同时控制应用进程、Keychain
和 SQLite 的本地管理员仍可替换全部状态。高保证场景应把审计实时复制到独立、追加式
存储；第一版有意不包含该能力。

### 原始查询结果

第一版不脱敏。MCP 客户端可能把生产数据发送给云端模型，Token 创建必须显式确认风险。
网关无法控制结果离开 HTTP 响应之后的缓存、会话留存或模型供应商策略。

### 异常断连与取消

网页使用独立取消请求，MCP 在调用被中断时会用预生成的 `queryId` 补偿取消。若原始
REST 客户端被强制杀死、网络栈没有及时向 Servlet 容器报告断连，服务端不保证在同一
瞬间收到通知；此时 JDBC 查询超时（每数据源 5–30 秒）仍是最终保险丝。不要把关闭
一个裸 HTTP 客户端等同于已经确认数据库查询取消，应通过查询状态或审计事件核对。

## 本机与网络边界

- 默认只监听 `127.0.0.1`；不要随意使用反向代理、端口转发或共享 macOS 账号。
- 非回环地址只有在 `gateway.remote-enabled=true` 且 Spring TLS 已启用时才可启动。
  第一版没有团队 RBAC、OIDC 或远程 MCP，因此不建议启用远程模式。
- `scripts/run.sh` 拒绝缺失、不可执行或符号链接形式的 Keychain helper；Java 进程还会
  校验 owner、写权限和运行期摘要。
- Application Support 运行目录为 `0700`，未初始化的 `bootstrap-token` 为 `0600`
  并在成功设置密码后删除。
- Keychain 锁定、macOS 屏幕锁和磁盘加密仍属于必要的宿主机控制。

## 数据库账号建议

- 每个生产库单独建立只读账号，只授予业务对象的 `SELECT`/必要 `SHOW VIEW`。
- 不授予 `FILE`、`EXECUTE`、`GRANT OPTION`、`SUPER`、`ALTER SYSTEM`、用户/角色管理、
  任意对象写权限或未知动态权限。
- 把账号来源限制到网关主机，配置短连接超时并使用可验证服务端证书。
- 先在非生产库验证连接和查询防线，再连接生产；即使网关不阻断，也不要用 root/SYS
  做“临时测试”。

## 密钥与 Token 处置

- MCP Token 仅放在客户端的私有环境配置，不写入仓库、命令行参数或聊天消息。
- 为每个 AI 客户端签发独立、最小数据源范围、短有效期 Token，停止使用后立即吊销。
- Token 泄露时吊销并重新签发；数据库凭据疑似泄露时在数据库端轮换，并在网页更新后
  重新做连接检查。
- 不备份未加密的 `gateway.db` 或客户端 Token 配置。Keychain 与数据库备份策略由
  macOS 管理员单独制定。

## 漏洞报告

请只用本地测试库复现。报告中不要包含生产地址、账号、密码、Token、完整 SQL 或查询
结果；可以提供安全错误码、审计 `queryId`、数据库类型和脱敏后的最小语句。
